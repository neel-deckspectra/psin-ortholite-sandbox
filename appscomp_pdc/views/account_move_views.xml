<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Add "Asset Category" to supplier invoices -->
    <record model="ir.ui.view" id="view_invoice_asset_category">
        <field name="name">account.invoice.supplier.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_line_ids']/tree/field[@name='account_id']" position="before">
                <field string="Asset Category"
                       name="asset_category_id" domain="[('type','=','purchase')]"
                       context="{'default_type':'purchase'}"/>
            </xpath>
<!--            attrs="{'column_invisible': [('parent.move_type', '!=', 'in_invoice')]}"-->
            <xpath expr="//field[@name='line_ids']/tree/field[@name='account_id']" position="after">
                <field string="Asset Category" name="asset_category_id" invisible="1"/>
            </xpath>
            <xpath expr="//sheet/div[@name='button_box']" position="inside">
                <!--                <div class="oe_button_box" name="button_box">-->
                <button name="get_invoice_pdc_draft_payment"
                        class="oe_stat_info"
                        icon="fa-money"
                        type="object">
                    <field name="draft_payment_count" widget="statinfo"/>
                    <!--                        <span class="oe_stat_text">Payment Reference</span>-->
                </button>
                <!--                </div>-->
            </xpath>
            <field name="invoice_date" position="after">
                <field name="payment_ids" widget="many2many_tags" readonly="0"/>
            </field>
        </field>
    </record>

</odoo>
